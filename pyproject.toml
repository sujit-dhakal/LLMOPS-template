[project]
name = "llmops-template"
version = "0.1.0"
description = "Template for llmoops projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "python-dotenv>=1.2.2",
]
